document.getElementById('container').innerHTML = '<div class="row">Content Text</div>'; // Parsing, 

// elements
document.createElement('tag') -> object
parent.appendChild( element ) - adaugarea obiectului
parent.insertBefore( element, referenceElement);
parent.removeChild( element ) - stergerea obiectului
parent.replaceChild( element, oldElement)

// atributes
// standart .src, .style, .width, .href,
// .className -> class=""
element.setAttribute('name','value')
element.getAttribute('name')
element.removeAttribute('name')
